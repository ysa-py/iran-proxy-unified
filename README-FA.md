# 🇮🇷 سیستم نهایی پروکسی ایران - نسخه سازمانی ۳.۲.۰

![Build Status](https://img.shields.io/badge/build-passing-brightgreen)
![Version](https://img.shields.io/badge/version-3.2.0-blue)
![Iran Optimized](https://img.shields.io/badge/Iran-Optimized-red)

**سیستم پیشرفته و سازمانی مدیریت پروکسی که به طور ویژه برای دور زدن فیلترینگ پیشرفته DPI و زیرساخت‌های فیلترینگ اینترنت ایران بهینه‌سازی شده است.**

## 🌟 ویژگی‌های کلیدی

### بهینه‌سازی‌های ویژه ایران

این سیستم شامل تکنیک‌های پیشرفته دور زدن فیلترینگ است که به صورت اختصاصی برای زیرساخت فیلترینگ ایران طراحی شده‌اند. پشتیبانی از پروتکل Reality برای استتار پیشرفته TLS، استفاده از xhttp Transport برای مخفی‌سازی ترافیک HTTP، پیاده‌سازی SNI Fragmentation برای دور زدن فیلتر SNI، جعل امضای TLS برای تقلید از ترافیک قانونی HTTPS، و اعتبارسنجی چند نقطه‌ای برای تست اتصال در سراسر اپراتورهای اینترنت ایران از جمله این ویژگی‌ها هستند.

### عملکرد و قابلیت اطمینان

سیستم دارای توزیع هوشمند بار با تقسیم خودکار بین پروکسی‌های موجود، مدیریت تطبیقی تایم‌اوت که به صورت پویا بر اساس شرایط شبکه تنظیم می‌شود، سیستم پشتیبان چند لایه برای جایگزینی خودکار هنگام مسدود شدن پروکسی‌های اصلی، قابلیت‌های خودترمیمی برای بازیابی خودکار از خرابی‌های اتصال، و پیاده‌سازی الگوی Circuit Breaker برای جلوگیری از خرابی‌های متوالی است.

### نظارت و تحلیل

سیستم نظارت جامع شامل امتیازدهی سلامت در زمان واقعی با ارزیابی مداوم کیفیت پروکسی، آمار دقیق در مورد نرخ موفقیت و عملکرد، شناسایی خودکار الگوهای فیلترینگ و مشکلات شبکه، تحلیل تاریخی اثربخشی پروکسی، و گزارش تضمین کیفیت با تولید خودکار معیارهای کیفیت است.

### امنیت و حریم خصوصی

سیستم هیچ گونه لاگ یا ذخیره‌سازی اطلاعات کاربر یا اطلاعات اتصال ندارد. تمامی تنظیمات پروکسی رمزگذاری شده‌اند. کد به طور کامل متن‌باز و شفاف است. ممیزی‌های امنیتی منظم از طریق اسکن خودکار در خط لوله CI/CD انجام می‌شود.

## راهنمای نصب سریع

### پیش‌نیازها

برای استفاده از این سیستم به Go نسخه ۱.۲۱ یا بالاتر، Git برای کلون کردن مخزن، و Make برای استفاده از Makefile (اختیاری) نیاز دارید.

### نصب از سورس

برای نصب، ابتدا مخزن را کلون کنید با دستور git clone، سپس وارد دایرکتوری شوید، و با استفاده از make build یا به صورت دستی با رفتن به پوشه src و اجرای go build، برنامه را بسازید.

### استفاده از Docker

برای استفاده از Docker، ابتدا تصویر را با docker build بسازید، سپس کانتینر را با docker run اجرا کنید.

## راهنمای استفاده

### استفاده پایه

برای اجرا با بهینه‌سازی‌های پیش‌فرض ایران از دستور iran-proxy-ultimate با فلگ iran-mode استفاده کنید. برای حداکثر دور زدن DPI از سطح maximum استفاده کنید. برای تنظیمات سفارشی می‌توانید پارامترهای مختلف مانند تعداد اتصالات همزمان، تایم‌اوت، پروتکل‌ها، و سایر ویژگی‌ها را مشخص کنید.

### حالت‌های مختلف عملکرد

سیستم سه حالت عملکردی دارد. حالت سرعت برای حداکثر اتصالات همزمان و پردازش سریع‌تر، حالت متعادل برای ترکیب بهینه عملکرد و ثبات، و حالت کیفیت برای حداکثر قابلیت اطمینان و بهترین کیفیت پروکسی.

## تنظیمات پیشرفته

### پارامترهای خط فرمان

سیستم از پارامترهای متعددی پشتیبانی می‌کند. فلگ iran-mode برای فعال‌سازی بهینه‌سازی‌های ویژه ایران، dpi-evasion-level برای کنترل سطح تکنیک‌های دور زدن DPI با گزینه‌های standard، aggressive، و maximum، performance-mode برای انتخاب بین حالت‌های speed، balanced، و quality، و سایر پارامترها برای کنترل اتصالات همزمان، تایم‌اوت، انتخاب پروتکل، نظارت، خودترمیمی، و سیستم پشتیبان.

### متغیرهای محیطی

برای استقرارهای کانتینری، سیستم تنظیمات را از متغیرهای محیطی می‌خواند که امکان استقرار انعطاف‌پذیر بدون تغییر کد را فراهم می‌کند. تنظیمات اصلی شامل IRAN_MODE، DPI_EVASION_LEVEL، و PERFORMANCE_MODE هستند.

## یکپارچه‌سازی GitHub Actions

این پروژه شامل یک گردش کار جامع GitHub Actions است که به صورت خودکار بررسی‌های کیفیت کد و امنیت را اجرا می‌کند، برنامه را می‌سازد و تست می‌کند، تنظیمات پروکسی را تولید و بهینه‌سازی می‌کند، تنظیمات را بر اساس برنامه به‌روزرسانی می‌کند (هر ۱۵ دقیقه، هر ۴ ساعت، و روزانه)، گزارش‌های جامع سلامت ارائه می‌دهد، و تصاویر Docker را می‌سازد و منتشر می‌کند.

### زمان‌بندی اجرای خودکار

گردش کار در زمان‌های مختلفی اجرا می‌شود. به‌روزرسانی‌های سریع پیکربندی هر ۱۵ دقیقه انجام می‌شود. بررسی‌های جامع پروکسی هر ۴ ساعت صورت می‌گرفته. تحلیل عمیق روزانه در ساعت ۳ بامداد به وقت تهران (۲۳:۰۰ UTC) انجام می‌شود. بررسی‌های بازیابی اضطراری هر ساعت اجرا می‌شوند.

### راه‌اندازی دستی

می‌توانید گردش کار را به صورت دستی با گزینه‌های سفارشی‌سازی گسترده شامل حالت بررسی، تنظیمات عملکرد، سطح دور زدن DPI، و موارد دیگر راه‌اندازی کنید.

## استقرار Docker

### استفاده از Docker Compose

یک فایل docker-compose.yml ایجاد کنید که سرویس اصلی برنامه را با تمام تنظیمات لازم، متغیرهای محیطی، حجم‌های ذخیره‌سازی، و پیکربندی سلامت تعریف می‌کند. سپس با docker-compose up اجرا کنید.

### استقرار Kubernetes

برای محیط‌های تولید، پروژه شامل مانیفست‌های استقرار Kubernetes با مجموعه‌های تکرار برای دسترسی بالا، درخواست‌ها و محدودیت‌های منابع برای زمان‌بندی مناسب، پیکربندی متغیر محیطی برای انعطاف‌پذیری، و یکپارچه‌سازی با زیرساخت Kubernetes موجود است.

## معماری سیستم

### اجزای اصلی

سیستم بر اساس یک معماری ماژولار با جداسازی واضح نگرانی‌ها ساخته شده است. بررسی‌کننده پروکسی اتصال پروکسی را اعتبارسنجی و عملکرد را در چندین نقطه شامل اپراتورهای ایرانی اندازه‌گیری می‌کند. تولیدکننده پیکربندی تنظیمات بهینه‌شده برای پروتکل‌های مختلف با بهبودهای ویژه ایران ایجاد می‌کند. موتور دور زدن DPI تکنیک‌های متعدد فرار شامل تکه‌تکه‌سازی SNI، جعل امضای TLS، و مخفی‌سازی پروتکل را پیاده‌سازی می‌کند.

### جریان داده

سیستم این جریان کلی را دنبال می‌کند: واکشی منابع پروکسی، اعتبارسنجی اتصال، اعمال بهینه‌سازی‌های ایران، تولید نمرات سلامت، ایجاد پیکربندی‌های بهینه‌شده، نظارت بر عملکرد، و فعال‌سازی خودترمیمی در صورت نیاز.

## عیب‌یابی

### مشکلات رایج

اگر پروکسی‌ها کار نمی‌کنند، مطمئن شوید که حالت ایران با iran-mode فعال است. سعی کنید سطح دور زدن DPI را به maximum افزایش دهید. بررسی کنید که آیا اپراتور شما پورت‌های خاصی را مسدود کرده است. حالت fallback را برای افزونگی بیشتر فعال کنید.

برای نرخ موفقیت پایین، تایم‌اوت را با پارامتر timeout تنظیم کنید. اتصالات همزمان را با max-concurrent کاهش دهید. خودترمیمی را با enable-self-healing فعال کنید. حالت عملکرد را به quality برای ثبات بهتر تغییر دهید.

### لاگ‌ها و اشکال‌زدایی

لاگ‌گذاری مفصل را با فلگ verbose فعال کنید. معیارها را در فایل metrics/quality-metrics.json بررسی کنید. آمار را در stats/proxy-stats.json مرور کنید.

## مشارکت در پروژه

مشارکت‌ها خوشایند و قدردانی می‌شوند. فرآیند شامل fork کردن مخزن، ایجاد یک شاخه ویژگی، انجام تغییرات، اجرای تست‌ها، اجرای linter، commit کردن تغییرات، push به شاخه، و باز کردن یک Pull Request است.

### استانداردهای کد

از بهترین شیوه‌های Go پیروی کنید. برای عملکرد جدید تست بنویسید. مستندات را در صورت نیاز به‌روزرسانی کنید. اطمینان حاصل کنید که تمام بررسی‌های CI پاس می‌شوند. برای منطق پیچیده توضیحات اضافه کنید.

## مجوز

این پروژه تحت مجوز MIT منتشر شده است که امکان استفاده، اصلاح، و توزیع رایگان را با حفظ اعتبار نویسندگان اصلی فراهم می‌کند.

## قدردانی

توسعه این سیستم از کار جوامع V2Ray و Xray در پیاده‌سازی پروتکل، مشارکت‌کنندگان در تحقیقات فیلترینگ پروکسی ایران، و ابزارها و کتابخانه‌های امنیتی متن‌باز بهره برده است.

## پشتیبانی

برای مشکلات، سوالات، یا پیشنهادات می‌توانید یک issue در GitHub باز کنید، مستندات موجود در دایرکتوری docs را بررسی کنید، issue‌های بسته شده را برای مشکلات مشابه مرور کنید، و فایل README را برای اطلاعات مرجع سریع مشاهده کنید.

---

**توسعه داده شده با ❤️ برای جامعه ایرانی**

**نسخه:** ۳.۲.۰ نسخه نهایی  
**آخرین به‌روزرسانی:** ۱۴۰۲

---

## آمار

سیستم به طور خودکار آمار شامل تعداد کل پروکسی‌های تست شده، درصد نرخ موفقیت، میانگین زمان پاسخ، پروتکل‌های پشتیبانی شده، نرخ موفقیت دور زدن DPI، و برچسب زمانی آخرین به‌روزرسانی را تولید و به‌روزرسانی می‌کند.

برای تحلیل دقیق، دایرکتوری‌های stats و metrics را بررسی کنید.
